# 机构数据导入演示套件

按以下顺序使用“机构数据导入”页面上传：

1. `germplasm.csv` — 种质资源；
2. `environment.json` — 环境；
3. `pedigree.csv` — 系谱；
4. `phenotype.csv` — 试验/表型；
5. `genotype.vcf` — 基因型；
6. `literature.txt` — 公开文献。

这套数据使用一致的材料编号和环境编号。完成后可使用
`HNNF-G001`、`HNNF-G002` 或 `ENV-SY-2026` 检查实体与关系追溯。
