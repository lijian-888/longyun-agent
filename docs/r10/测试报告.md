# R10 测试报告

测试日期：2026-09-20。环境：`longyun.e-farmer.cn` / `longyun-agent` Compose 项目。测试中禁止清空或销毁真实生产数据。

| 验收点 | 检查方法 | 结果 |
| --- | --- | --- |
| AC10.1 共享 Keycloak、控制面、前端、API | 检查容器、控制面 `/healthz`、公网首页/API/Keycloak | 通过：控制面容器 healthy，全部共享业务容器继续运行；三个公网检查均 HTTP 200 |
| AC10.2 存储策略与队列命名空间 | `control_plane.py reconcile` + `check`；核对机构配置与队列任务分组 | 通过：1 个 active 机构，`longyun-hnnf` 无匿名 Bucket Policy，机构业务库 `longyun_hnnf`；队列命名空间 `hainan-nanfan`，有 1 个 paused 任务；所有探针为 true |
| AC10.3 备份/销毁脚本 | 实际备份、SHA-256 校验；销毁脚本干运行与错误确认令牌拒绝 | 通过：最终 `20260920T015856Z` 备份完成并复验，目录 0700；错误令牌和缺少生产额外参数均以退出码 1 拒绝。未销毁生产数据 |
| AC10.4 六类交付物 | 核对 [交付清单.md](交付清单.md) | 六类文件均已交付；账号清单不含口令，试用复盘仅涵盖技术试运行 |
| AC10.5 回滚 | 只回滚 R10 sidecar，验证核心业务不受影响，再恢复 sidecar | 通过：执行 `rollback.sh --execute` 后仅控制面被移除，其余 8 个业务容器运行；恢复后控制面 healthy，公网首页/API/Keycloak 仍均 HTTP 200；审计日志记录回滚 |

备份清单含 `rice_demo`（包括 Keycloak schema）、`longyun_hnnf`、数据库角色元数据，以及 MinIO、原始文件、研究文件、ACPs 状态、Keycloak 数据卷和 R10 审计卷。该备份含密钥与证书，不上传仓库。

限制：线上真实数据毁损恢复未作演练；应在隔离环境定期完成数据库与 MinIO 恢复演练。控制面状态并不能替代端到端登录和业务功能测试。当前 AI 队列命名空间由数据库的 `institution_id` 表示，全局并发限制仅适用于单 API 进程；尚未提供多副本分布式队列隔离。正式试用反馈见 [试用复盘.md](试用复盘.md)。
