# 账号清单（不含口令）

Keycloak 领域：`rice-research`。以下为 2026-09-20 生产领域的已启用账号盘点；密码、恢复码和服务凭据不进入文档或 Git。

| 账号 | 用途 | 管理方式 |
| --- | --- | --- |
| `li.researcher`、`wang.researcher` | 科研人员演示 | Keycloak 领域角色 `researcher`；课题权限由后端进一步限制 |
| `zhang.processor` | 数据处理员演示 | Keycloak 领域角色 `data_processor` |
| `chen.fieldadmin` | 字段管理员演示 | Keycloak 领域角色 `field_admin` |
| `acps.researcher`、`svc-longyun-acps-test` | ACPs 对接 / 服务测试 | 仅限登记的外部对接范围；上线前复核角色和密钥轮换 |

管理员账号不列出用户名。日常业务账号不得持有 MinIO 根密钥、PostgreSQL 管理口令或控制面容器访问权。新增/停用账号通过 Keycloak 管理端完成，并记录审批人、时间、角色及课题授权。演示口令由账号管理员通过安全渠道单独交付；首次使用后修改，不得复用服务器凭据。
